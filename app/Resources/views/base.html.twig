<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>Darren's takeaway.com assignment</title>
        <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet"/>
        <link href="{{ asset("assets/css/vendors.css") }}" rel="stylesheet" />
        <link href="{{ asset("assets/css/style.css") }}" rel="stylesheet" />
    </head>
    <body class="takeaway-body">
        <div class="menu">
            <div class="container">
                <nav class="navbar navbar-toggleable-md navbar-expand-md navbar-light" id="commRollover">
                    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                        <form class="form-inline my-2 my-lg-0">
                            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" id="restaurant-search">
                        </form>
                        <ul class="navbar-nav">
                            <li class="nav-item active">
                                <a class="nav-link" href="/">Home<span class="sr-only">(current)</span></a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Sorting
                                </a>
                                <div class="dropdown-menu sorting-values-menu" aria-labelledby="navbarDropdown">
                                    {% for sortValue in sortingValues %}
                                        <a class="dropdown-item {{ (sortingValueDefault == sortValue)? "active" : "" }}" href="#" data-sort="{{ sortValue }} ">{{ sortValue }}</a>
                                    {% endfor %}
                                </div>
                            </li>

                        </ul>
                    </div>
                </nav>
            </div>
        </div>
        <div class="container">
            {% block body %}{% endblock %}
        </div>
        <script src="{{ asset("assets/js/vendors.js") }}"></script>
        <script src="{{ asset("assets/js/main.js") }}"></script>
    </body>
</html>
